# Server
server.port=8080

# MySQL DataSource
spring.datasource.url=jdbc:mysql://localhost:3306/artforge_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Mahanth@9826
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# JWT
jwt.secret=artforge-super-secret-key-2024-min-32-chars!!
jwt.expiration=86400000

# CORS allowed origins (add your Vercel URL here)
cors.allowed.origins=http://localhost:3000,http://localhost:5173,https://full-stack-project-2-lilac.vercel.app

# -------------------------------------------------------------
# REAL EMAIL CONFIGURATION (Gmail)
# -------------------------------------------------------------
# 1. Put your real Gmail address below
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=dupaguntlarajamahanthprasad@gmail.com

# 2. Put your Google "App Password" below (NOT your real password!)
spring.mail.password=rxlnlsiedxqnrrkd

# Security Settings for Gmail
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
